" LITERALLY EVERYTHING BEFORE THIS POINT SHOULD JUST BE FROM THE NORMAL
" VIMRC
" source $HOME/.dotfiles/vim/vimrc

if has("gui_macvim")
    " ----- Start MacVim in fullscreen
    set fuoptions=maxvert,maxhorz
    au GUIEnter * set fullscreen
endif

" ----- Linux gVim Screen Config
set guioptions-=T
set guioptions-=m
set guioptions-=r
set guioptions-=b

set guifont=Inconsolata:h16
colorscheme panda 
" (IF IT ISN'T INSTALLED FIRST THEN INSTALL IT! GET IT HERE: https://github.com/markvincze/panda-vim)
